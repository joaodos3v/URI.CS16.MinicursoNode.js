 - Boa prática em JS: começa declarando suas 'variáveis' como `const`. Se precisar, altera para `let`. Mas utilizar `const` no código é a melhor prática.

 - `console.time('chave')`
    - `console.timeEnd('chave')`